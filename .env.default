HYDRA_FULL_ERROR=1
LOGS_DIR="./logs"
PROPAGATE_DBX_CREDS="0" # Set to 1 to propagate dbx credentials to the workers in the mulit-node setup (required for mlflow logging from workers)
AIM_ENDPOINT="aim://aimstack-server.appgrowth.com:53800" # delete this var to use local aim server
